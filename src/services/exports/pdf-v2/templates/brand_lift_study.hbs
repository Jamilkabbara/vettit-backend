{{!-- Pass 48 Phase 3 — brand lift body. Renders the SAME shared canonical
     report body as general_research.hbs (structural parity is the Pass-48
     requirement). The brand-lift methodology's instrument — the FULL
     exposed-vs-control funnel (every stage, not one numeric stage) — is the
     centerpiece section inside _canonical_body.hbs, fed from
     report.centerpiece (built from mission.analysis.funnel). --}}
{{> canonicalBody}}
