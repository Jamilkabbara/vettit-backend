<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{mission.title}}, VETT Report</title>
  <style>{{{fontFaceCss}}}</style>
  <style>{{{baseCss}}}</style>
</head>
<body>

  {{!-- ─── COVER (shared across all mission types) ─────────────────── --}}
  <section class="page cover">
    <div class="cover-glow"></div>

    <div class="cover-header">
      <div class="wordmark">VETT</div>
      <div class="cover-tagline">AI-Powered Market Research</div>
    </div>

    <div class="cover-title-block">
      <h1 class="cover-title">{{mission.title}}</h1>
      {{#if mission.brief}}
      <p class="cover-brief">{{mission.brief}}</p>
      {{/if}}

      <div class="cover-meta">
        <div class="cover-meta-key">Respondents</div>
        <div class="cover-meta-val">{{mission.respondent_count}}</div>
        <div class="cover-meta-key">Mission completed</div>
        <div class="cover-meta-val">{{missionCompletedLabel}}</div>
        <div class="cover-meta-key">Report generated</div>
        <div class="cover-meta-val">{{reportGeneratedLabel}}</div>
        <div class="cover-meta-key">Mission ID</div>
        <div class="cover-meta-val">{{mission.id}}</div>
      </div>
    </div>

    <div class="cover-footer">
      Generated by VETT · vettit.ai
    </div>
    {{!-- Public methodology page. Sits BESIDE the simulation-honesty
         disclaimer (see the Methodology section in the body), never in
         place of it, so a second-hand reader can audit the figures. --}}
    <div class="cover-footer cover-footer-method">
      Methodology · {{methodologyUrl}}
    </div>
  </section>

  {{!-- ─── BODY (mission-type specific) ────────────────────────────────────────
       Wrapped in .page-frame so the thead/tfoot supply a page inset that REPEATS
       on every printed page (Chromium re-draws table headers/footers per page).
       Without this, a section that overflows a page resumed flush against the
       physical top edge. See .page-frame in _base.css. --}}
  <table class="page-frame">
    <thead><tr><td></td></tr></thead>
    <tfoot><tr><td></td></tr></tfoot>
    <tbody><tr><td>
      {{> body}}
    </td></tr></tbody>
  </table>

</body>
</html>
