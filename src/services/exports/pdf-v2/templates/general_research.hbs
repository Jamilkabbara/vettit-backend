{{!-- Pass 48 Phase 3 — general research body. Renders the shared canonical
     report body (identical to brand_lift_study.hbs). All section logic lives
     in _canonical_body.hbs so every methodology's PDF is structurally the
     same: exec summary → headline metrics → centerpiece → key findings →
     the full survey (correct scales) → data-quality notes → disclaimer. --}}
{{> canonicalBody}}
